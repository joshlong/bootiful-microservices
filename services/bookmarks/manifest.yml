---
applications:
- name: bookmarks
  memory: 512M
  instances: 1
  host: bookmarks-${random-word}
  domain: cfapps.io
  path: target/bookmarks.jar
  services:
    - bookmarks-postgresql
    - config
    - eureka
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"