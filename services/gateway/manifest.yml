---
applications:
- name: gateway
  memory: 512M
  instances: 1
  host: gateway-${random-word}
  domain: cfapps.io
  path: target/gateway.jar
  services:
    - config
    - eureka
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
    debug: "true"